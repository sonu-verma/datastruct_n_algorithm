# Resolving Array Problems

✅ Beginner (Basic concepts — 10 questions):
1 Create an array of numbers and print them. - Done
Find the sum of all elements in an array.
Find the largest and smallest elements in an array.
Reverse an array without using built-in functions.
Count occurrences of a specific value in an array.
Merge two arrays into one.
Check if a value exists in an array.
Remove duplicates from an array.
Find the second largest number in an array.
Sort an array without using sort() function.

✅ Intermediate (Problem-solving with arrays — 10 questions):
Rotate an array by k positions.

Find pairs in an array whose sum equals a given number.

Move all zeroes to the end without changing order of non-zero elements.

Find the missing number from a sequence array.

Find the intersection of two arrays.

Flatten a multi-dimensional array.

Separate even and odd numbers into different arrays.

Find all unique combinations of numbers that sum up to a given target.

Write a function that returns the difference between two arrays.

Find continuous subarray with the maximum sum (Kadane’s Algorithm).

✅ Advanced (Complex manipulations — 10 questions):
Merge intervals from an array of ranges (example: [[1,3],[2,6],[8,10]]).

Generate all permutations of an array.

Generate all subsets of an array.

Group anagrams from an array of strings.

Given an array, find the longest increasing subsequence.

Rearrange an array so that positive and negative numbers alternate.

Write a function to shuffle an array randomly.

Find the frequency of each element in an array without using built-in functions.

Given an array with duplicates, return all elements that appear more than once.

Implement a sliding window algorithm on an array for finding maximum/minimum sum.

✅ Expert (System-level problem-solving — 10 questions):
Build a prefix-sum array and use it to calculate range sums efficiently.

Solve "Maximum product subarray" problem.

Solve "Trapping rainwater" problem.

Implement array-based LRU Cache logic.

Solve the "Dutch National Flag" problem (sort 0s, 1s, 2s without extra space).

Design a custom array-based queue and stack.

Implement binary search on a rotated sorted array.

Solve "Find the majority element" using Moore’s voting algorithm.

Write a function that finds the maximum XOR of two numbers in an array.

Implement a 2D matrix rotation in-place (clockwise or anticlockwise).